## Environment Template
# Copy this file to `.env` and fill in real values. Do NOT commit the actual `.env`.

## Client (Vite) vars (must start with VITE_ to be exposed in frontend bundle)
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=unsigned_raw_vault
# Locks master password input after first successful unlock ("true" or "false")
VITE_LOCK_MASTER_PASSWORD=false
# Backend API base URL (for GitHub Pages deployment, point to your Render backend)
# VITE_API_BASE_URL=https://your-app-name.onrender.com

## Server-only vars (never prefix with VITE_)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_key
CLOUDINARY_API_SECRET=your_secret
CLOUDINARY_UPLOAD_PRESET=unsigned_raw_vault

# Mongo connection (create a dedicated least-privileged user)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=safe_vault

# JWT secret (generate a long random string in production)
JWT_SECRET=change_me_dev

## Optional
# morgan log format: dev | combined | tiny | etc.
LOG_FORMAT=dev

## Notes
# - /api proxy in Vite removes need for VITE_API_BASE_URL so it's omitted.
# - Never commit real secrets (Cloudinary API key/secret, JWT, Mongo creds).
# - Regenerate JWT_SECRET & rotate Mongo user if accidentally exposed.
# - Remove any legacy Firebase variables; they are not used anymore.
